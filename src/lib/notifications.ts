/**
 * TypeScript resolves this file for `import '…/notifications'`.
 * Metro uses `notifications.native.ts` (iOS/Android) and `notifications.web.ts` (web) when present.
 */
export * from './notifications.native';
