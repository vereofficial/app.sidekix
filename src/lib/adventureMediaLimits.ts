/** Adventure compose: duration is ms (expo-image-picker `duration`). */
export const ADVENTURE_MAX_VIDEO_MS = 30_000;

/** Max upload size for a single photo or video on new-adventure (bytes). */
export const ADVENTURE_MAX_MEDIA_BYTES = 75 * 1024 * 1024;
