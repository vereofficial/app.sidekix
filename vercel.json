{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/p/:postId", "destination": "/api/share-html?postId=:postId" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
